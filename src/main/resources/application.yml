spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/zzzsheep
    username: zzzsheep
    password: ?!Pan2005929
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

plaid:
  client-id: your_plaid_client_id
  secret: your_plaid_secret
  environment: sandbox

server:
  port: 8080